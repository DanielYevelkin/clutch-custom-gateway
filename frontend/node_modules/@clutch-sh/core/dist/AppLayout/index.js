import React from "react";
import { Outlet } from "react-router-dom";
import styled from "@emotion/styled";
import { Grid as MuiGrid } from "@mui/material";
import Loadable from "../loading";
import Drawer from "./drawer";
import Header, { APP_BAR_HEIGHT } from "./header";
const AppGrid = styled(MuiGrid)({
    flex: 1,
});
const ContentGrid = styled(MuiGrid)({
    flex: 1,
    maxHeight: `calc(100vh - ${APP_BAR_HEIGHT})`,
});
const MainContent = styled.div({ overflowY: "auto", width: "100%" });
const AppLayout = ({ isLoading = false, configuration = {} }) => {
    return (React.createElement(AppGrid, { container: true, direction: "column", "data-testid": "app-layout-component" },
        React.createElement(Header, Object.assign({}, configuration)),
        React.createElement(ContentGrid, { container: true, wrap: "nowrap" }, isLoading ? (React.createElement(Loadable, { isLoading: isLoading, variant: "overlay" })) : (React.createElement(React.Fragment, null,
            React.createElement(Drawer, null),
            React.createElement(MainContent, null,
                React.createElement(Outlet, null)))))));
};
export default AppLayout;
