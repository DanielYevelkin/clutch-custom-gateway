import React from "react";
import { HappyEmoji } from "../Assets/emojis";
import { Button } from "../button";
import { useAppContext } from "../Contexts";
import { FeatureOn, SimpleFeatureFlag } from "../flags";
import Grid from "../grid";
import Paper from "../paper";
import styled from "../styled";
import { Typography } from "../typography";
const StyledPaper = styled(Paper)({
    padding: "0px 16px",
    "@media screen and (max-width: 970px)": {
        padding: "8px 16px",
    },
});
/**
 * An NPS Feedback Banner which will open up the NPSHeader component to ask for feedback
 */
export const Banner = ({ elevated = true, icon = React.createElement(HappyEmoji, null), feedbackText = "Enjoying this feature? We would like your feedback!", feedbackButtonText = "Give Feedback", defaultFeedbackOption, }) => {
    const { triggerHeaderItem } = useAppContext();
    const banner = (React.createElement(Grid, { container: true, direction: "row", spacing: 1, alignItems: "center", justifyContent: "center" },
        icon && (React.createElement(Grid, { item: true },
            React.createElement("div", { "data-testid": "nps-banner-icon", style: { display: "flex" } }, icon))),
        React.createElement(Grid, { item: true },
            React.createElement(Typography, { "data-testid": "nps-banner-text", variant: "body2" }, feedbackText)),
        React.createElement(Grid, { item: true },
            React.createElement(Button, { id: "nps-banner-button", "data-testid": "nps-banner-button", variant: "neutral", text: feedbackButtonText, size: "xsmall", onClick: () => triggerHeaderItem("NPS", { defaultFeedbackOption }) }))));
    return elevated ? React.createElement(StyledPaper, { "data-testid": "nps-banner-container" }, banner) : banner;
};
/**
 * An NPS Feedback Banner which will ask the user for feedback then open up the NPSHeader
 * NOTE: requires the NPSHeader to be enabled
 */
const FeedbackBanner = ({ ...props }) => (React.createElement(SimpleFeatureFlag, { feature: "npsHeader" },
    React.createElement(FeatureOn, null,
        React.createElement(Banner, Object.assign({}, props)))));
export default FeedbackBanner;
