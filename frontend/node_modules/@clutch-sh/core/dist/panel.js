import React from "react";
import styled from "@emotion/styled";
import ExpandMoreIcon from "@mui/icons-material/ExpandMore";
import { Accordion as MuiExpansionPanel, AccordionDetails, AccordionSummary, Typography, } from "@mui/material";
const FullWidthExpansionPanel = styled(MuiExpansionPanel) `
  width: 100%;
`;
/** TODO: Combine with accordion */
const Accordion = ({ heading, summary, expanded, children }) => {
    return (React.createElement(FullWidthExpansionPanel, { defaultExpanded: expanded },
        React.createElement(AccordionSummary, { expandIcon: React.createElement(ExpandMoreIcon, null) },
            React.createElement(Typography, null, heading),
            React.createElement("div", { style: { flexGrow: 1 } }),
            React.createElement(Typography, { color: "secondary" }, summary)),
        React.createElement(AccordionDetails, null, children)));
};
export default Accordion;
