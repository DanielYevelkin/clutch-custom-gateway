import * as React from "react";
import type { SelectProps as MuiSelectProps } from "@mui/material";
interface BaseSelectOptions {
    label: string;
    value?: string;
    startAdornment?: React.ReactElement;
}
export interface SelectOption extends BaseSelectOptions {
    group?: BaseSelectOptions[];
}
export interface SelectProps extends Pick<MuiSelectProps, "disabled" | "error" | "value"> {
    defaultOption?: number | string;
    helperText?: string;
    label?: string;
    name: string;
    options: SelectOption[];
    onChange?: (value: string) => void;
}
declare const Select: ({ defaultOption, disabled, error, helperText, label, name, options, onChange, value, }: SelectProps) => JSX.Element;
export interface MultiSelectProps extends Pick<MuiSelectProps, "disabled" | "error"> {
    defaultOptions?: Array<number> | Array<string>;
    helperText?: string;
    label?: string;
    name: string;
    selectOptions: SelectOption[];
    chipDisplay?: boolean;
    onChange?: (values: Array<string>) => void;
}
declare const MultiSelect: ({ defaultOptions, disabled, error, helperText, label, name, selectOptions, chipDisplay, onChange, }: MultiSelectProps) => JSX.Element;
export { Select, MultiSelect };
