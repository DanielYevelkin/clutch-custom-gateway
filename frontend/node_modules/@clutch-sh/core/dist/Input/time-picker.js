import React from "react";
import { TimePicker as MuiTimePicker } from "@mui/x-date-pickers";
import { AdapterDayjs } from "@mui/x-date-pickers/AdapterDayjs";
import { LocalizationProvider } from "@mui/x-date-pickers/LocalizationProvider";
import styled from "../styled";
import { TextField } from "./text-field";
const PaddedTextField = styled(TextField)({
    // This is required as TextField intentionally unsets the right padding for
    // end adornment styles since material introduced it in v5
    // Clutch has TextFields with end adornments that are end aligned (e.g. resolvers).
    ".MuiInputBase-adornedEnd": {
        paddingRight: "14px",
    },
});
const TimePicker = ({ onChange, ...props }) => (React.createElement(LocalizationProvider, { dateAdapter: AdapterDayjs },
    React.createElement(MuiTimePicker, Object.assign({ renderInput: inputProps => React.createElement(PaddedTextField, Object.assign({}, inputProps)), onChange: (value) => {
            if (value && value.isValid()) {
                onChange(value.toDate());
            }
        } }, props))));
export default TimePicker;
