/// <reference types="react" />
import type { ToggleButtonGroupProps as MuiToggleButtonGroupProps } from "@mui/lab";
export { ToggleButton } from "@mui/material";
export interface ToggleButtonGroupProps extends Pick<MuiToggleButtonGroupProps, "value" | "children" | "size" | "orientation" | "onChange"> {
    /** If true, multiple children options can be selected simultaneously. */
    multiple?: boolean;
}
declare const ToggleButtonGroup: ({ multiple, value, children, size, orientation, onChange, }: ToggleButtonGroupProps) => JSX.Element;
export default ToggleButtonGroup;
