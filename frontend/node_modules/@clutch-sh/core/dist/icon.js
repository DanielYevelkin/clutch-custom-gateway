import React from "react";
import styled from "@emotion/styled";
import FiberManualRecordTwoToneIcon from "@mui/icons-material/FiberManualRecordTwoTone";
import { Grid } from "@mui/material";
const StyledStatusIcon = styled(FiberManualRecordTwoToneIcon) `
  ${({ ...props }) => `
    color: ${props["data-color"]}
  `}
`;
export const AvatarIcon = styled.img({
    borderRadius: "50%",
    maxHeight: "100%",
    maxWidth: "initial",
});
export const StatusIcon = ({ children, variant = "neutral", align = "left", ...props }) => {
    let justifyContent = "flex-start";
    if (align === "right") {
        justifyContent = "flex-end";
    }
    else if (align === "center") {
        justifyContent = "center";
    }
    return (React.createElement(Grid, Object.assign({ container: true, alignItems: "center", justifyContent: justifyContent }, props),
        variant === "neutral" && (React.createElement(React.Fragment, null,
            React.createElement(StyledStatusIcon, { "data-color": "#C2C8F2" }),
            " ",
            children)),
        variant === "success" && (React.createElement(React.Fragment, null,
            React.createElement(StyledStatusIcon, { "data-color": "#69F0AE" }),
            " ",
            children)),
        variant === "failure" && (React.createElement(React.Fragment, null,
            React.createElement(StyledStatusIcon, { "data-color": "#FF8A80" }),
            " ",
            children))));
};
